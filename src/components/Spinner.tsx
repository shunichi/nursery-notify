import React, { FunctionComponent } from 'react';

export const Spinner: FunctionComponent = () => {
  return <div id="spinner" className="text-center mt-5"><i className="fas fa-spinner fa-spin fa-3x"></i></div>;
};
